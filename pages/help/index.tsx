import type { NextPage } from 'next';

import styles from './index.module.scss';

const Help: NextPage = () => {
	return (
		<main className="main">
			<h1>Help</h1>
		</main>
	);
};

export default Help;
