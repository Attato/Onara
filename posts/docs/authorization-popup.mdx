---
id: 9
title: Authorization Popup
lastUpdated: 25-05-2023
readingTime: 1 min
category: Templates
---

# Authorization Popup

The Authorization Popup template is a reusable component used for displaying a popup window with options for user authorization using different services. It is implemented using Next.js and NextAuth.js.

<CodeBlock language="tsx" code={`<AuthorizationPopup />`}/>

<AuthorizationPopup />


## Props 

<CodeBlock language="md" code={`| Prop          	| Type      	| Default Value                	| Description                                   	|
|---------------	|-----------	|------------------------------	|-----------------------------------------------	|
| buttonContent 	| ReactNode 	| <button>Blank popup</button> 	| The button content of the authorization popup 	|
| title         	| string    	| Untitled                     	| The content of the authorization popup        	|`} />


## Example
Here's an example usage of the Authorization Popup component:

<CodeBlock language="tsx" code={`<AuthorizationPopup
    title="Sign In to Continue"
    buttonContent="Sign In"
    buttonContent={
        <button style={{
            backgroundColor: "#0f0f0f",
            border: "1px solid #232323",
            borderRadius: "6px",
            padding: "12px 16px",
           display: "flex",
           alignItems: "center",
           gap: "4px",
           color: "#fefefe"
        }}>
            Example
            <GiftIcon width={16} height={16}/>
        </button>
    }
/>`} />

Result:

<AuthorizationPopup
    title="Sign In to Continue"
    buttonContent="Sign In"
    buttonContent={
        <button style={{
            backgroundColor: "#0f0f0f",
            border: "1px solid #232323",
            borderRadius: "6px",
            padding: "12px 16px",
            display: "flex",
            alignItems: "center",
            gap: "4px",
            color: "#fefefe"
        }}>
            Example
            <GiftIcon width={16} height={16}/>
        </button>
    }
/>

In the above example, when the user clicks on the "Sign In" button, the popup window will be displayed with options to sign in using GitHub or GitLab.

That's it! You can now use the Authorization Popup component in ONARA application to provide a convenient way for users to sign in using different services.